/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: ["localhost", "avatar.iran.liara.run"]
    },
    reactStrictMode: false
}

export default nextConfig
